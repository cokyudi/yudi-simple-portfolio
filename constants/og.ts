// Bump this when OG image rendering changes so CDNs / social scrapers refetch.
// The OG routes send immutable cache headers, so the URL must change to bust them.
export const OG_VERSION = '2026-06-10';
